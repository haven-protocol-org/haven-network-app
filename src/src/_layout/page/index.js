// Library Imports
import React from "react";

// Relative Imports
import { Container } from "./styles";

const Page = ({ children }) => {
  return <Container>{children}</Container>;
};

export default Page;
